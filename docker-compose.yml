version: '2.1'

services:
  api:
    image: pingbot
    build: ./api
    tty: true
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
    links:
      - mongo
    depends_on:
      - mongo
  mongo:
    container_name: mongo
    image: mongo    
    ports:
      - "27017:27017"